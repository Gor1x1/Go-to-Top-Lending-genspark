# ============================================
# Go to Top — Environment Variables
# ============================================
# Скопируйте этот файл в .dev.vars для локальной разработки
# cp .env.example .dev.vars
#
# ⚠️ НЕ коммитьте .dev.vars в git!
# ============================================

# --- Cloudflare (для деплоя) ---
# Получить на https://dash.cloudflare.com/profile/api-tokens
# CLOUDFLARE_API_TOKEN=your_cloudflare_api_token

# --- Admin Auth ---
# Текущие значения захардкожены в src/lib/auth.ts
# В будущем перенести в Cloudflare Secrets (wrangler secret put)
# ADMIN_PASSWORD=gototop2026
# JWT_SECRET=gtt-admin-jwt-secret-2026

# --- Telegram Bot ---
# Настраивается через админку: /admin → Telegram Bot
# Или прямой доступ через API: /api/admin/telegram-bot
# TELEGRAM_BOT_TOKEN=your_bot_token
# TELEGRAM_CHAT_ID=your_chat_id

# --- D1 Database ---
# Конфигурация в wrangler.jsonc
# Локальная разработка автоматически использует SQLite через --local флаг
# DATABASE_NAME=gototop-production
# DATABASE_ID=8b74a4aa-4685-48c4-9e99-eaa7d8aae3e2
